__pycache__/
*.pyc
.pytest_cache/
.dut_snapshot/
*.pcap
report.html
coverage_matrix.txt
wheelhouse/

# core dumps (never commit)
*.core.gz
*.core
core.*

# private plugin layer — proprietary product klish CLI grammar + Broadcom SDKLT chip tables (not public)
plugins/

# runtime run/issue reports (generated by pytest_terminal_summary)
run_issues*.md

# macOS Finder metadata
.DS_Store
**/.DS_Store
